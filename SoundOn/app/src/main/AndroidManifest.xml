<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.soundon">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- android:label="@string/app_name" -->
    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"

        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.SearchMusicActivity" />
        <activity android:name=".view.PlayActivity" />
        <activity android:name=".view.TopListActivity" />
        <activity android:name=".view.MusicListActivity" />
        <activity android:name=".view.SingerInfoActivity" />
        <activity android:name=".view.AlbumMusicActivity" />
        <activity android:name=".view.AboutActivity"></activity>
    </application>

</manifest>